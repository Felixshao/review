# _*_ config: utf-8 _*_
# python3菜鸟教程数字

import operator

print(operator.ge(3, 2))    # 判断左边值是否大于等于右边值，正确返回true，否则false
print(operator.gt(3, 2))    # 判断左边值是否大于右边值
print(operator.eq(2, 2))    # 判断左边值是否等于右边值
print(operator.le(2, 2))    # 判断左边值是否小于等于右边值
print(operator.lt(1, 2))    # 判断左边值是否小于右边值